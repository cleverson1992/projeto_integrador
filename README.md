Para criar o ambiente virtual use o comando :

```
python -m venv .venv
```

Para ativar o ambiente virtual use o comando:

```
.\.venv\Scripts\activate
```

Para instalar o Django no Ambiente virtual use o comando:

```
pip install django 
```